#include <stdio.h>

int main() {
    int n = 18;
    long long factorial = 1;

    for (int i = 1; i <= n; i++) {
        factorial *= i;
    }

    printf("The factorial of %d is %lld", n, factorial);

    return 0;
}
