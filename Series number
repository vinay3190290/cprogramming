#include <stdio.h>

int main() {
   int i, n;

   printf("Enter the number of terms in the series: ");
   scanf("%d", &n);

   printf("The series is: ");
   for(i = 1; i <= n; i++) {
      printf("%d ", i);
   }

   return 0;
}
